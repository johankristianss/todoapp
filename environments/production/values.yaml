# Production Environment Configuration

app:
  name: todo

# Backend configuration
backend:
  replicas: 3
  image:
    repository: johan/todo-backend
    tag: latest
  flaskEnv: production

# Frontend configuration
frontend:
  replicas: 3
  image:
    repository: johan/todo-frontend
    tag: latest

# Production domain - change to your assigned domain
domain: todo.ltu-m7011e-johan.se

# Email for Let's Encrypt certificates
email: johan.kristiansson@ltu.se

# Environment identifier
environment: Production

# Ingress configuration
ingress:
  class: traefik
  # Use production issuer for real certificates
  certIssuer: letsencrypt-prod
